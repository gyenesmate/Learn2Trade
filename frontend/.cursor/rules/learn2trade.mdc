---
description: Learn2Trade frontend project guide — where to look and global conventions
alwaysApply: true
---

# Learn2Trade Frontend Guide

Use this rule as the map of the frontend. Prefer the smallest change that matches existing patterns.

**File placement source of truth:** [`docs/FILE_STRUCTURES.md`](../../docs/FILE_STRUCTURES.md)  
**UI/UX source of truth:** [`docs/TRADING_UI_CONTEXT.md`](../../docs/TRADING_UI_CONTEXT.md)

Also follow `angular-expert.mdc`, `material-expert.mdc`, and `agents.md`.

## Architecture (summary)

```text
AppComponent (minimal root + global hosts)
  └── RouterOutlet
       └── BaseLayoutComponent
            ├── NavbarComponent
            ├── SidebarComponent
            └── RouterOutlet → features/*
```

Do not put sidebar/navbar in `AppComponent`. Do not use `*-page` or `app-shell` naming. Details and placement rules: `docs/FILE_STRUCTURES.md`.

## Stack (verify in `package.json`)

- Angular + Material + CDK ^22
- RxJS 7.8, Zone.js present
- Vitest (`ng test`); Playwright e2e
- Charts: `lightweight-charts`; notifications: custom `app-snackbar` stack via `NotificationService`

## Path aliases

| Alias | Maps to |
| --- | --- |
| `@core/*` | `./src/app/core/*` |
| `@shared/*` | `./src/app/shared/*` |
| `@features/*` | `./src/app/features/*` |
| `@styles/*` | `./src/styles/*` |

Same-folder `./` imports may stay relative.

## Where to look

| Area | Path |
| --- | --- |
| App root / routes / config | `src/app/app.*` |
| Shell | `src/app/core/layout/{base-layout,sidebar,navbar}/` |
| Services / utils / guards / models / env | `src/app/core/{services,utils,guards,models,env}/` |
| Shared UI | `src/app/shared/components/` |
| Features | `src/app/features/{dashboard,markets,trading,portfolio,auth,system}/` |
| Design tokens / theme | `src/styles/` (`_tokens`, `theme`, `_material-overrides`, `_components`, `tailwind`) |

### Feature ↔ URL (URLs must stay stable)

| Feature | Routes |
| --- | --- |
| dashboard | `/dashboard` |
| markets | `/home`, `/admin/crypto-currencies/...` |
| trading | `/crypto/:id` |
| portfolio | `/profile`, `/edit-profile` |
| auth | `/login`, `/register`, `/banned` |
| system | `/testing-ground`, `**` |

### Docs

| Path | Role |
| --- | --- |
| `docs/FILE_STRUCTURES.md` | Structure & placement rules |
| `docs/TRADING_UI_CONTEXT.md` | Trading UI design system |
| `docs/features/` | Planned per-feature guides |
| `docs/OLD_DESCRITIONS/` | Legacy only |

Visual direction: dark-first trading terminal. Semantic tokens only. Layout via Tailwind-style flex/grid utilities in `styles/tailwind.scss` / `_utilities.scss` (SCSS helpers — not the Tailwind npm package).

## Guidelines

1. Read the target feature + one similar feature before editing.
2. Place new files per `FILE_STRUCTURES.md` ownership rules.
3. Preserve auth/guard behavior and route URLs.
4. Reuse `shared/components` and `core/services` before inventing new abstractions.
5. Feature-specific dialogs/components stay under the feature.
6. Styles: tokens + Material overrides globally; layout via flex/grid utilities; local SCSS for brand/surface only.
7. Validate with `npm run build` / `npm run test:unit` when practical.
